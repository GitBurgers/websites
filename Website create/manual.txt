||||HTML||||
<p id or class = "Name">Text!</p>
<button on|click="FunctionName()">Button Text</button>

    <div class="grid-1">
        <div class="square"></div>
    </div>

||||CSS||||
color -- rbd()
font-size -- put 15px
font-family -- type of font
font-weight -- boldness
font-style  -- italic or normal
text-align  -- Align text (left, center, right)
text-decoration -- Underline, line-through, none
letter-spacing  -- Space between letters
line-height -- Space between lines of text
text-transform  -- Uppercase, lowercase, capitalize

width, height   -- size of elements
padding	Space   -- inside the box (inside the border)
margin  -- Space outside the box
border  -- Outline around the box
box-sizing  -- Controls how padding/border are included in size

Div (square)
  width: _px;
  height: _px;
  background-color: red;
  border-radius: 10px; /* Try 50% for a circle */
  box-shadow: 5px 5px 15px gray;
  opacity: 0.7; /* 0 = invisible, 1 = full */
  margin: 20px;
  padding: 10px;
  background: linear-gradient(to right, red, yellow);


||||JS||||
document.getElementById("ID").innerText = "new text here";

function FunctionName() {
  document.getElementById("greeting").innerText = "Hi there!";
}

document.getElementById("ID").style.color = "blue";
document.getElementById("ID").style.fontSize = "24px";